/*var x = 5;
var y = 10;
x += y; // é a mesma coisa que --> x = x + y (15)
x -= y; // é a mesma coisa que --> x = x - y (-5)
x *= y; // é a mesma coisa que --> x = x * y (50)
x /= y; // é a mesma coisa que --> x = x / y (0.5)
x %= y; // é a mesma coisa que --> x = x % y (0)
x **= y;// é a mesma coisa que --> x = x ** y (9765625)

var num = 20

num += 10
console.log(num)

//operador ternário
//condição ? true : false
 var idade = 30
 var doente = 'não'

 var podeBeber

 podeBeber = (idade >= 18) && (doente === 'não') ? 'Pode beber' : 'Não pode beber'

 console.log(podeBeber)*/

 // Some 500 ao valor de scroll abaixo,
// atribuindo o novo valor a scroll
var scroll = 1000;
scroll += 500;
console.log(scroll)

// Atribua true para a variável darCredito,
// caso o cliente possua carro e casa.
// E false caso o contrário.
var possuiCarro = true;
var possuiCasa = true;
var darCredito;

darCredito = (possuiCarro && possuiCasa) ? true : false
console.log(darCredito) 
